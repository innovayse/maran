{
  "//": [
    "Non-secret defaults for maran-api. This file ships with the product and is world-readable.",
    "SECRETS NEVER LIVE HERE (rules/security.md): the encryption key and the database connection",
    "string come from /etc/maran/panel.env (root:panel 0640), written by the installer and",
    "loaded as environment variables. Anything an operator may safely see and tune belongs here."
  ],
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    }
  },
  "Database": {
    "Host": "/var/run/postgresql",
    "Database": "maran",
    "Username": "panel"
  },
  "Agent": {
    "SocketPath": "/run/maran/agent.sock",
    "ProbeTimeoutSeconds": 2
  },
  "RateLimiting": {
    "LoginMaxAttempts": 5,
    "LoginWindowSeconds": 60,
    "LoginLockoutSeconds": 300,
    "ApiPermitLimit": 300,
    "ApiWindowSeconds": 60
  },
  "AllowedHosts": "*"
}
