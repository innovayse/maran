<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <!-- Lets the pins below reach packages we never reference directly. JasperFx.RuntimeCompiler
         asks for a Roslyn several minor versions behind the one our analysers already pull in, and
         the resulting split fails the build under warnings-as-errors. -->
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>
  <ItemGroup>
    <PackageVersion Include="Npgsql" Version="9.0.5" />
    <PackageVersion Include="Microsoft.CodeAnalysis.BannedApiAnalyzers" Version="5.6.0" />
    <PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="9.0.19" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="4.0.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
    <PackageVersion Include="Google.Protobuf" Version="3.36.1" />
    <PackageVersion Include="Grpc.Net.Client" Version="2.83.0" />
    <PackageVersion Include="Grpc.Tools" Version="2.83.0" />
    <PackageVersion Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="9.0.4" />
    <PackageVersion Include="JasperFx.RuntimeCompiler" Version="5.0.0" />
    <!-- Transitive, pinned to one Roslyn version across the whole graph (see the property above).
         Every id here ships from the same Roslyn build and declares exact (= x.y.z) dependencies on
         its siblings, so the whole set moves together or not at all: pinning a subset makes the
         unpinned siblings resolve older and fails restore with NU1608. 5.3.0 is the ceiling on this
         target framework — Microsoft.CodeAnalysis 5.6.0 and later drop their net9.0 assets and pull
         .NET 10 runtime libraries into the graph. -->
    <PackageVersion Include="Microsoft.CodeAnalysis.Common" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp.Workspaces" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.Workspaces.Common" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.Workspaces.MSBuild" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp.Scripting" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.Scripting.Common" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.VisualBasic" Version="5.3.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.VisualBasic.Workspaces" Version="5.3.0" />
    <PackageVersion Include="WolverineFx" Version="6.33.0" />
    <PackageVersion Include="WolverineFx.Postgresql" Version="6.33.0" />
    <!-- Runs each command's FluentValidation validator before its handler. Without it the
         validators in every module are dead code: they compile, they are tested, and nothing
         calls them — a weak password reached the database before this was noticed. -->
    <PackageVersion Include="WolverineFx.FluentValidation" Version="6.33.0" />
    <PackageVersion Include="NetArchTest.Rules" Version="1.3.2" />
    <PackageVersion Include="Testcontainers.PostgreSql" Version="4.14.0" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="9.0.19" />
    <PackageVersion Include="Microsoft.Extensions.Options.DataAnnotations" Version="9.0.19" />
    <PackageVersion Include="Microsoft.Extensions.Http.Resilience" Version="10.9.0" />
    <PackageVersion Include="Microsoft.Extensions.Resilience" Version="10.9.0" />
    <PackageVersion Include="Serilog.AspNetCore" Version="9.0.0" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore" Version="9.0.19" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Design" Version="9.0.19" />
    <PackageVersion Include="FluentValidation" Version="12.1.1" />
    <!-- Discovers each module's validators from its own assembly, so a module that adds one does
         not also have to remember to register it. -->
    <PackageVersion Include="FluentValidation.DependencyInjectionExtensions" Version="12.1.1" />
    <!-- Test-only: FluentValidation's TestHelper namespace (shipped inside the main package
         since FluentValidation 9) drives CreateAccountCommandValidatorTests' per-property
         assertions; Microsoft.EntityFrameworkCore.InMemory backs
         CreateAccountCommandHandlerTests/ListAccountsQueryHandlerTests as a fast, deterministic
         AccountsDbContext double so those unit tests need no PostgreSQL container. -->
    <!-- Argon2id is the mandated password KDF (rules/security.md item 9). Konscious is the
         reference managed implementation; .NET ships no Argon2 of its own, and PBKDF2
         (Rfc2898DeriveBytes) is not an acceptable substitute for a system being built today. -->
    <!-- JWT bearer authentication for the panel API (spec §10). 9.0.19 matches every other
         Microsoft pin here; the 10.x line targets .NET 10 and drops its net9.0 assets. -->
    <PackageVersion Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="9.0.19" />
    <!-- The signing half of the same story, referenced directly by the Identity module: JwtBearer
         validates tokens, JsonWebTokens creates them. Ahead of the 8.19.2 that JwtBearer 9.0.19
         resolves on its own: the IdentityModel packages take a minimum-version dependency on each
         other rather than an exact one, so lifting this one lifts the family with it and the graph
         stays consistent — verified by a clean restore, not assumed. -->
    <PackageVersion Include="Microsoft.IdentityModel.JsonWebTokens" Version="8.22.0" />
    <!-- RFC 6238 TOTP for the panel's second factor (spec §10). A small, dependency-free
         implementation of exactly the algorithm; writing our own would break rules/security.md
         item 9 ("no home-grown crypto") for no gain. -->
    <PackageVersion Include="Otp.NET" Version="1.4.1" />
    <PackageVersion Include="Konscious.Security.Cryptography.Argon2" Version="1.3.1" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.InMemory" Version="9.0.19" />
    <!-- MailKit: the panel's only SMTP client, reached from exactly one file
         (Maran.Modules.Monitoring/Services/SmtpMailer.cs). .NET's own System.Net.Mail.SmtpClient is
         documented obsolete by its own authors and does not implement modern submission — STARTTLS
         negotiation, implicit TLS on 465, SASL — so building on it would mean writing those parts
         by hand, which rules/security.md item 9 forbids for anything security-relevant. MimeKit
         comes with it and composes the message. -->
    <PackageVersion Include="MailKit" Version="4.17.0" />
  </ItemGroup>
</Project>
